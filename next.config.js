/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['pbs.twimg.com'],
    },
    // Include any other configurations you might have here
  };
  
  module.exports = nextConfig;
  